source 'https://cdn.cocoapods.org/'

platform :ios, "9.0"
use_frameworks!

target "TPPDF_Example" do
    pod "TPPDF", :path => "../"

    target "TPPDF_Tests" do
        inherit! :search_paths

        pod "Quick"
        pod "Nimble"
    end
end
